# Gas Town Fixes Status
Last updated: 2026-05-11

## Goal
Fix the end-to-end Gas Town flow where a spec can be handed to agents who autonomously implement it.

## Constraints
- Use GT_SESSION_TRANSPORT=nats
- Rig: testgt2
- If code changes in /home/stevef/dev/freeride/gastown: run gt down, then make install in gastown, then gt up in /home/stevef/gt

## Completed Fixes

### 1. GT_TOWN_ROOT Priority Bug
- **Files**: molecule_status.go, hook.go (3 places), molecule_attach.go, nudge.go
- **Issue**: GT_ROOT was taking priority over GT_TOWN_ROOT, breaking identity detection
- **Fix**: Changed detection order to prioritize GT_TOWN_ROOT (set by daemon) over GT_ROOT
- **Symptom**: "No work" issue - GT_ROOT was set to /home/stevef/dev/freeride instead of /home/stevef/gt

### 2. Hook Directory Resolution Bug
- **File**: mail_identity.go
- **Issue**: Incorrect filepath.Dir() call on rig path, returning wrong directory
- **Fix**: Removed filepath.Dir() call, returning resolvedDir directly
- **Symptom**: Session identity resolution failures

### 3. CLAUDE.md Runtime Path Filter
- **File**: git.go
- **Issue**: CLAUDE.md in runtime path causing false positives on uncommitted changes
- **Fix**: Added CLAUDE.md to runtime path filter

### 4. gt done Guard
- **File**: done.go
- **Issue**: Polecats calling gt done without implementing anything
- **Fix**: Added code-level guard that blocks completion if only auto-save commits exist (requires real implementation commits)

### 5. Polecat Formula Steps
- **File**: templates/polecat-CLAUDE.md
- **Issue**: Polecats skipping formula steps
- **Fix**: Added "FORMULA STEPS - DO NOT SKIP" section to template

### 6. Built and Installed
- **Action**: Ran make install with SKIP_UPDATE_CHECK=1
- **Result**: Installed gt binary v1.0.1-318-g8240de7a-dirty

### 7. Service Restart
- **Action**: Ran gt down (stopped all services) and gt up (restarted all services)
- **Result**: All services running: NATS, Dolt (port 3307), Daemon, Deacon, Mayor, Planner, Mechanic, Witness, Refinery, Architect, QA

## Verification
- Polecat sessions start correctly
- Identity resolves correctly (testgt2/polecats/rust)
- Hook shows work
- Task created → slung to polecat → polecat sees work → gt done requires implementation commit

## Mechanic Patrol
- Mechanic searches ALL session logs in /home/stevef/gt/logs/sessions/*.log including polecats
- Searches for "Extraordinary action" pattern and nudges affected agent

## Relevant Files
- /home/stevef/dev/freeride/gastown/internal/cmd/molecule_status.go
- /home/stevef/dev/freeride/gastown/internal/cmd/hook.go
- /home/stevef/dev/freeride/gastown/internal/cmd/mail_identity.go
- /home/stevef/dev/freeride/gastown/internal/cmd/done.go
- /home/stevef/dev/freeride/gastown/internal/cmd/git.go
- /home/stevef/dev/freeride/gastown/templates/polecat-CLAUDE.md
- /home/stevef/gt/logs/sessions/: Session logs for debugging agent behavior

## Next Steps
- Test full end-to-end flow with a real implementation task
- Verify polecat follows formula steps in order (load-context → implement → commit → gt done)

## Additional Issues Found

### 8. Mechanic Nudge Missing Rig Prefix (FIXED)
- **File**: cmd/gt-agent/main.go (mechanicPatrolScript)
- **Issue**: Mechanic nudges agents without rig prefix (e.g., "architect" instead of "testgt2/architect")
- **Fix**: Changed nudge to use "$rig/$agent" format
- **Status**: Fixed

### 9. Mechanic Scanning Stale Logs (FIXED)
- **File**: cmd/gt-agent/main.go (mechanicPatrolScript)
- **Issue**: Mechanic scans old logs from different rigs (e.g., hq-qsq-testgt1 from May 7)
- **Fix**: Added filtering to only check logs from current rig and skip logs older than 60 minutes
- **Status**: Fixed

### 10. Polecat Template Improved (FIXED)
- **File**: templates/polecat-CLAUDE.md, internal/templates/polecat-CLAUDE.md
- **Issue**: Polecat not implementing - jumping straight to gt done after gt prime
- **Fix**: Added explicit "IMPLEMENTATION MEANS CREATING FILES" section with concrete example workflow
- **Status**: Fixed

### 11. gt done Guard Blocks Auto-Save Only Commits (FIXED)
- **File**: internal/cmd/done.go
- **Issue**: Polecat could gt done with only auto-save commits (no real implementation)
- **Fix**: Check top commit after auto-save - if only auto-save, block completion with clear instructions
- **Status**: Working - polecat now gets blocked and retries

### 12. Model Selection Issue (EXPERIMENT 1)
- **Symptom**: Polecat uses 11B model instead of 70B
- **Root Cause**: freeride proxy selecting model based on candidates, X-GasTown-Role header may not be reaching freeride
- **Evidence**: freeride_live.log shows "meta/llama-3.2-11b-vision-instruct" being used
- **Fix**: Debug X-GasTown-Role header in gt-agent LLM client

### 13. Retry Limit + Escalation (EXPERIMENT 2)
- **Symptom**: Polecat stuck in infinite retry loop when blocked by gt done guard
- **Fix**: Add max retries (e.g., 5) then escalate to witness with details

### 14. More Aggressive Template (EXPERIMENT 4)
- **Symptom**: LLM doesn't understand it needs to implement - just runs gt prime then gt done
- **Fix**: Add explicit step-by-step workflow directly in template user-facing section
- **Status**: Fixed

### 15. TDD Workflow & Planner Granularity (FIXED)
- **Files**: internal/formula/formulas/mol-polecat-work-tdd.formula.toml, internal/templates/roles/planner.md.tmpl, internal/formula/formulas/mol-idea-to-plan.formula.toml, cmd/gt-agent/main.go
- **Issue**: Polecats skip work; Planner tasks are too abstract.
- **Fixes**: 
    - [x] Fixed critical bug in `BdCmd` helper (exec: Stdout already set).
    - [x] Verified `gt sling` dispatch to rig polecats is functional.
    - [x] Fixed critical bug in `gt prime` where formula steps were not being resolved/expanded (Item 21).
    - [x] Verified `mol-polecat-work-tdd` now expands correctly into 12 steps for polecats.
### 16. Debugging Polecat Inactivity (FIXED)
- **Symptom**: Polecats start sessions but do not implement work, often skipping to `gt done` without creating files.
- **Root Cause**: Polecats were not seeing formula steps (TDD checklist) in `gt prime` output due to a resolution bug (Item 21). Without instructions, they defaulted to completion.
- **Status**: Fixed via Item 21.

### 17. Molecule Steps Not Materializing (VERIFIED)
- **Root Cause**: The molecule `te-wisp-gjly` (mol-polecat-work-tdd) is a root-only wisp. Steps are read inline from the formula at prime time.
- **Verification**: Confirmed that `gt prime` is the authoritative source for polecat instructions. Steps now appear inline after Item 21 fix.
- **Status**: Verified.

### 18. Verifying Polecat Sees Formula Steps (FIXED)
- **Action**: Ran `gt prime` in polecat worktree.
- **Result**: Confirmed that formula steps (including TDD cycle) are now fully rendered and visible to the agent.
- **Status**: Fixed.

### 19. Polecat Implementation Follow-Through (STABLE)
- **Observation**: Once formula steps are visible, polecats can follow the TDD workflow (Load context → Set up branch → Write failing tests → etc.).
- **Status**: Stable.

### 20. Polecat Not Seeing Formula Steps (FIXED)
- **Status**: Resolved by Item 21.

### 21. **[SOLVED]** Polecats doing nothing (testgt2 rig).
    - **Symptom**: `gt prime` rendered empty work instructions for `mol-polecat-work-tdd`.
    - **Technical Root Cause**: Formula composition (`extends`, `expand`) was being parsed but not *resolved* during the `gt prime` output phase. The formula parser returns a structure representing the inheritance tree, but the renderer was only looking at the top-level (empty) steps slice.
    - **Fix**: Integrated `formula.Resolve` into `internal/cmd/prime_molecule.go` (specifically in `showFormulaSteps` and `showFormulaStepsFull`). This ensures that the inheritance tree is flattened into a final actionable checklist before rendering to the agent.
    - **Verification**: Confirmed via `gt prime --hook` that the 12-step TDD checklist (inherited from `mol-polecat-work`) is now correctly injected into the agent's context.
    - **Current Status**: Fixed. Polecats in `testgt2` now receive the full TDD workflow instructions and can proceed autonomously.

22. **[FIXED]** Polecat TDD Autonomous Execution.
    - Verified `testgt2/polecats/chrome` implementation of `te-syu` (Math Add).
    - **Status**: Verified.

23. **[CLEANUP]** Rig testgt2 reset for fresh trial.
    - **Action**: Nuclear shutdown and removal of `testgt2` rig; reset town beads to OPEN.
    - **Verification**: Full unit test suite is 100% green.
    - **Status**: Ready for new trials.

24. **[FIXED]** Agent Command Hallucinations (gt done / gt mol attach).
    - **Symptom**: Mayor/Planner/Architect were trying to run `gt done` or `gt mol attach <bead-id>`.
    - **Root Cause**: Global `PRIME.md` template included a "Session Close Protocol" meant for polecats only, and `gt mol status` included placeholder instructions for missing molecules.
    - **Fix**: Removed "Session Close Protocol" from global `PRIME.md` (polecats get it via `polecat-CLAUDE.md`). Removed placeholder instructions from `internal/cmd/molecule_status.go`. Deleted all existing `PRIME.md` files to force re-provisioning.
    - **Status**: Fixed and Verified.

25. **[FIXED]** Agent Command Failures (nudge/mail/prime).
    - **Symptom**: Agents were trying to run standalone `nudge` or `mail` commands (missing `gt` prefix) or including parentheses in `gt prime` commands.
    - **Fix**: Implemented command normalization in `cmd/gt-agent/main.go` to automatically prefix `nudge`/`mail` with `gt` and strip trailing descriptive text from `gt prime`/`gt hook` calls.
    - **Status**: Fixed and Verified.

26. **[FIXED]** Bracketed Placeholder Hallucinations (<id>, <epic-id>).
    - **Symptom**: Agents (especially Planner) were trying to execute literal shell commands with bracketed placeholders like `bd create ... --parent <epic-id>`.
    - **Fix**: 
        1. Sanitized all core role templates (base, mayor, architect, planner, qa) to replace `<...>` with `[...]` or descriptive text.
        2. Implemented a "Placeholder Kill-switch" in `cmd/gt-agent/main.go` that explicitly rejects any generated command containing both `<` and `>`.
    - **Status**: Fixed and Verified.

27. **[STABILIZED]** Unit Tests and Integration.
    - **Action**: Verified `internal/cmd` and `internal/doctor` suites are 100% green.
    - **Status**: Verified.

28. **[FIXED]** Announcement Hallucinations ("Mayor, checking in.").
    - **Symptom**: Agents were trying to execute their startup greetings as literal shell commands.
    - **Fix**: 
        1. Removed all "Announce" instructions from the Startup Protocol in `internal/cmd/prime_output.go`.
        2. Updated `normalizeGeneratedCommand` in `cmd/gt-agent/main.go` to treat "checking in" strings as shell NOPs (`true`) to safely ignore them if they still occur.
    - **Status**: Fixed and Verified.

29. **[FIXED]** Square-Bracket Placeholder Hallucinations (`[bead]`, `[rig]`).
    - **Symptom**: Mayor log showed `gt sling design --on [bead] [rig]/architect` was executed after the angle-bracket placeholder fix, causing `bead '[bead]' not found` and extraordinary retry behavior.
    - **Root Cause**: Role templates were sanitized from `<...>` to `[...]`, but the command kill-switch only rejected angle-bracket placeholders.
    - **Fix**:
        1. Extended `normalizeGeneratedCommand` in `cmd/gt-agent/main.go` to treat known square-bracket placeholders as shell NOPs (`true`) instead of executing them.
        2. Added unit coverage for both `[bead] [rig]` and `<bead-id>` generated commands.
        3. Reworded `internal/templates/roles/mayor.md.tmpl` so examples use `REAL-BEAD-ID` / `REAL-RIG` and explicitly forbid bracket placeholders.
    - **Verification**:
        1. `go test ./cmd/gt-agent` passes.
        2. Ran required deployment sequence after code changes: `gt down`, `make install`, `gt up`.
        3. Installed gt binary v1.0.1-334-g8bdbca8b-dirty.
    - **Status**: Fixed and installed; runtime log verification in progress.

30. **[FIXED]** `bd ready` Mis-Rewritten as `gt bd ready`.
    - **Symptom**: Architect log showed `bd ready` generated by the agent was normalized to `gt bd ready`, which printed `gt bead` help instead of ready work. This made the agent believe it had completed a patrol without seeing actionable beads.
    - **Root Cause**: `normalizeGeneratedCommand` used `strings.HasPrefix(trimmed, "bd read")`; the command `bd ready` also has that string prefix.
    - **Fix**: Added token-boundary command matching via `hasCommandPrefix`, so `bd read` no longer matches `bd ready`. Added unit coverage that `bd ready` remains unchanged.
    - **Verification**:
        1. `go test ./cmd/gt-agent` passes.
        2. Ran required deployment sequence after code changes: `gt down`, `make install`, `gt up`.
    - **Status**: Fixed and installed; runtime log verification in progress.

31. **[FIXED]** Fabricated Bead/Rig IDs in `gt sling --on` Commands.
    - **Symptom**: Mayor log showed the model invented fake handoff output (`MOL-1234`, `Rig-Alpha`) and then executed `gt sling design --on MOL-1234 Rig-Alpha/architect`, causing `bead 'MOL-1234' not found` and extraordinary retry behavior.
    - **Root Cause**: Placeholder guards stopped bracket/angle examples, but did not reject plausible-looking fake IDs generated from hallucinated `Output:` text.
    - **Fix**: Added `gt sling --on` preflight validation in `cmd/gt-agent/main.go`; the `--on` bead must look like a real lowercase Gas Town bead ID such as `te-ybw` or `hq-dehc`. Invalid generated slings become shell NOPs (`true`). Added unit coverage for rejected fake IDs and accepted real IDs.
    - **Status**: Fixed locally; test/install/restart verification pending.

32. **[FIXED]** Freeproxy town root mismatch causes rig parsing failure.
    - **Symptom**: `freeride_live.log` kept reporting `Town Root: /home/stevef/dev/freeride/gt` and `rigs.json not found`, so the proxy could not resolve `te-testgt2-architect` and the architect never moved beyond `bd mol current`.
    - **Fix**: Updated `test_dashboard_fetch.go` (the proxy smoke-test) to derive the town root from `GT_HOME`/`GT_TOWN_ROOT` with a sane `/home/stevef/gt` default, ensuring the proxy sees the real rigs directory where `rigs.json` lives.
    - **Status**: Fixed; restarts needed for the new path detection to take effect.

33. **[FIXED]** Invalid `gt mol execute --on` command from architect.
    - **Symptom**: Architect kept executing `gt mol execute --on mol-idea-9012` (and similar) after the hook was empty, which `gt mol` rejects (“unknown flag: --on”) and caused the patrol to loop without progressing.
    - **Fix**: Added a guard in `normalizeGeneratedCommand` to detect `gt mol execute --on …` and rewrite it to a harmless `true`, plus unit coverage showing the command no longer reaches the shell.
    - **Status**: Fixed and installed; architect now stays on-task without retrying the invalid command.

34. **[FIXED]** Molecule step IDs now rewrite to the attached wisp.
    - **Symptom**: The architect kept trying `bd close step-1`/`step-2` even though the molecule steps live under `te-wisp-zvrs/design/...`, so the command failed with `no issue found` and the workflow never advanced.
    - **Fix**: Tracked the last bead/molecule from `bd mol current`, rewrote subsequent `bd close step-*` commands to the fully qualified `te-wisp-zvrs/design/step-*`, and added a regression test ensuring the rewrite happens only when a molecule is known.
    - **Status**: Fixed, tested, and installed; architect can now close the right steps and proceed to `architecture.md` output.

35. **[FIXED]** Ambiguous `bd close <number>` commands now rewrite to the active step.
    - **Symptom**: Agents occasionally issued `bd close 1`/`bd close 2`, which `bd` treated as ambiguous and nothing happened; the workflow stalled even though we knew which step was active.
    - **Fix**: When a numeric `bd close` runs and we know the current step, rewrite the command to `bd close <te-wisp-id/...>` so `bd` stops bouncing on IDs like “1”. Added a unit test for the numeric rewrite.
    - **Status**: Fixed and installed—numeric closes now resolve without human disambiguation.

36. **[FIXED]** Agent tries to `bd close <num>` on a naked molecule, causing ambiguous ID loops.
    - **Symptom**: Architect saw a naked molecule (no active steps) and hallucinated `bd close 1`, which hit the database, caused an ambiguous ID error, and resulted in an "Extraordinary action" retry loop.
    - **Fix**: Actively intercept and reject shortcut close commands (like `bd close 1` or `bd close step-2`) when no step is active (`lastStepID` is empty). The rewrite logic now emits a harmless `true` shell command and logs a warning.
    - **Status**: Fixed, tested, and installed.

37. **[FIXED]** `gt sling --on` preflight validation fails to check if the bead actually exists.
    - **Symptom**: Agent hallucinated `gt sling design --on mol-1234 mayor`, which bypassed basic regex format checks but failed later because `mol-1234` didn't exist in the DB, causing another extraordinary loop.
    - **Fix**: Added deep validation to `hasInvalidSlingOnBead` which executes a background `gt bead show <id> --json` to verify the bead really exists in the DB before allowing the sling command to proceed. 
    - **Status**: Fixed and installed.

38. **[FIXED]** `gt prime` instructs agents to follow formula steps even when the formula has none (e.g. convoys).
    - **Symptom**: When slung a convoy formula (which only has parallel `legs`, not sequential `steps`), `gt prime` printed an empty checklist but still demanded "Work through ALL steps above, including submit and cleanup", causing the agent to get stuck in an endless `gt prime` loop hunting for steps that didn't exist.
    - **Fix**: Updated `showFormulaStepsFull` to return a boolean indicating if steps exist, and modified `gt prime` to only output the rigid step-by-step checklist demand if there are actual steps. Changed wording to cleanly instruct the agent to ignore molecule steps if the formula has none.
    - **Status**: Fixed and installed. Agent broke out of the loop and began executing.

39. **[FIXED]** `gt mail send` Syntax Errors.
    - **Symptom**: Mayor was trying to send status updates using positional arguments for subject and body, which the CLI rejects.
    - **Fix**: Updated `internal/templates/roles/base.md.tmpl` to explicitly show the required `-s` and `-m` flags in the communication rules.
    - **Status**: Fixed and Verified.

40. **[FIXED]** Agents stuck in `gt mail inbox` loops.
    - **Symptom**: Mayor was repeatedly listing its inbox but not reading the unread message containing the "Hello API" project spec.
    - **Fix**: 
        1. Updated `internal/templates/roles/base.md.tmpl` and `internal/templates/roles/mayor.md.tmpl` to explicitly mandate `gt mail read [id]` when unread mail is present.
        2. Updated the hardcoded **STARTUP PROTOCOL** in `internal/cmd/prime_output.go` for all roles to include the same `gt mail read` instruction.
    - **Status**: Fixed and installed.

### 41. [FIXED] Cross-Rig Bead Resolution in `gt prime`
- **File**: internal/cmd/prime.go
- **Issue**: Agents in rig-specific environments (like `testgt2/architect`) could not resolve town-level beads (e.g., `hq-*`), leading to "no issue found" errors during `gt prime`.
- **Fix**: Updated `outputBeadPreview` to use `resolveBeadDir` and `StripBeadsDir()` to explicitly bypass rig-specific environment shadowing when fetching bead details from the global town database.
- **Status**: Fixed and Verified.

### 42. [FIXED] Terminated Recursive Priming Loops
- **Files**: internal/cmd/prime_output.go, internal/beads/beads.go, internal/templates/roles/architect.md.tmpl
- **Issue**: Agents were entering infinite "prime-startup-prime" loops because their startup instructions included a mandate to run `gt prime`—which triggered the instruction print-out itself.
- **Fix**: Pruned the "Run `gt prime`" directive from startup protocols across all templates and hardcoded constants.
- **Status**: Fixed and Verified.

### 43. [FIXED] Resolved `bd show` / `bd ready` Loops
- **File**: internal/cmd/prime.go
- **Issue**: Agents were repeatedly running `bd show` or `bd ready` instead of proceeding to actionable work, even when bead details were already previewed in `gt prime`.
- **Fix**: Updated `outputAutonomousDirective` to replace the rigid "IMMEDIATELY run: bd show" with a direct instruction to run `gt sling [id] --formula shiny` for project beads.
- **Status**: Fixed and Verified.

### 44. [FIXED] `gt sling --formula` Ignored for Non-Polecat Targets
- **File**: `internal/cmd/sling.go`
- **Issue**: When slinging a bead to a coordinator (e.g., `mayor/` or `architect/`), the `--formula` flag was being ignored because the logic was hardcoded to only apply formulas automatically to polecat targets. This prevented the Architect from starting the "shiny" workflow.
- **Fix**: Initialized the `formulaName` variable from the `slingFormula` flag at the start of `runSling`, ensuring explicit formulas are respected for all target roles.
- **Status**: Fixed, Verified, and Installed.

### 45. [FIXED] Database Selector Pollution in `BdCmd`
- **Files**: `internal/cmd/sling.go`, `internal/cmd/sling_helpers.go`
- **Issue**: Agents slinging beads from a rig environment (like `testgt2/witness`) would fail to bond formulas to town-level beads (`hq-*`) because `bd` would inherit the rig's `BEADS_DIR` environment variable, ignoring the working directory routing.
- **Fix**: 
    1. Applied `.StripBeadsDir()` to all `BdCmd` calls in `sling_helpers.go` to ensure `bd` resolves the database correctly from its working directory.
    2. Migrated legacy `exec.Command("bd", ...)` calls in `sling.go` to the unified `BdCmd` builder to benefit from `StripBeadsDir()` and consistent environment management.
- **Status**: Fixed and Verified.

### 46. [FIXED] Agent "cat << EOF" commands failing due to strict placeholder check
- **File**: `cmd/gt-agent/main.go`
- **Issue**: Multiline strings and heredocs containing `<` and `>` (e.g. `cat << 'EOF' > file.md`) were triggering the `containsPlaceholder` hallucination guard, rewriting valid file creation commands to `true`.
- **Fix**: Replaced the naïve `strings.Contains("<")` check with a strict regex (`regexp.MustCompile("<[a-zA-Z0-9_-]+>")`) to only catch actual template placeholders like `<bead-id>`.
- **Status**: Fixed and Verified.

### 47. [FIXED] LLM hallucinating terminal `Output:` blocks
- **Files**: `cmd/gt-agent/main.go`, `internal/templates/roles/base.md.tmpl`
- **Issue**: The model was hallucinating entire interactive sessions (generating both `CMD:` and fake `Output:` blocks) in a single response, preventing the real terminal output from being fed back into the context loop.
- **Fix**: Added explicit instructions to the base template to stop generating after `CMD:`, and added a strict check in `gt-agent` to intercept and reject any response containing `Output:` with instructions to retry.
- **Status**: Fixed and Verified.

### 48. [FIXED] WorkDir resolution bug for non-polecat rig agents
- **File**: `cmd/gt-agent/main.go`
- **Issue**: `buildSystemPrompt` was incorrectly assigning rig-level agents (like the architect or QA) to the `/witness` directory by default instead of their own role directory.
- **Fix**: Updated the `default` fallback to correctly route to `filepath.Join(townRoot, rig, role)`.
- **Status**: Fixed and Verified.

### 49. [FIXED] Next Action `bd mol current` missing attached molecule ID
- **File**: `internal/cmd/molecule_status.go`, `cmd/gt-agent/main.go`
- **Issue**: The Next Action hint in `gt mol status` was suggesting `bd mol current [bead-id]` instead of the attached molecule ID. Also, `bd mol current` command rewriting was mishandling arguments.
- **Fix**: Updated `molecule_status.go` to reference `status.AttachedMolecule`, and fixed the rewrite logic in `gt-agent` to correctly append positional arguments.
- **Status**: Fixed and Verified.

### 50. [FIXED] Formula variables not substituted in architect's work
- **Files**: `internal/cmd/prime.go` (lines 969, 1004), `internal/cmd/prime_output.go` (line 805)
- **Issue**: Architect's formula steps showed `{{feature}}` placeholder instead of substituted value "Hello API". The code was using `strings.Split(attachment.FormulaVars, "\n")` which produced empty/nil slice, instead of using `attachment.AttachedVars` which contains the properly parsed variables.
- **Fix**: Changed all three call sites to pass `attachment.AttachedVars` (the parsed string slice) instead of `strings.Split(attachment.FormulaVars, "\n")`.
- **Status**: Fixed and Verified - `gt prime` now shows "Design Hello API" instead of "Design {{feature}}".

### 51. [FIXED] Architect SPEC.md path in template
- **File**: `internal/templates/roles/architect.md.tmpl`
- **Issue**: Architect couldn't find SPEC.md - looking in wrong location.
- **Fix**: Updated template to use proper path template variables `{{ .TownRoot }}/{{ .RigName }}/mayor/rig/SPEC.md`.
- **Status**: Fixed.

### 52. [FIXED] gt-agent only executes first LLM command
- **File**: `cmd/gt-agent/main.go`
- **Issue**: gt-agent was only executing the first CMD: block from LLM, ignoring subsequent commands.
- **Fix**: Changed code to collect and execute ALL CMD blocks in sequence.
- **Status**: Fixed.

### 53. [FIXED] LLM escaping quotes in heredoc
- **File**: `cmd/gt-agent/main.go`
- **Issue**: LLM outputs `\'EOF\'` instead of `'EOF'` causing heredoc to fail.
- **Fix**: Added string replacement to fix escaped quotes before execution.
- **Status**: Fixed.

### 54. [IN PROGRESS] Architect not creating architecture.md
- **Issue**: LLM puts heredoc content on same line as EOF, making file empty.
- **Fix**: Changed formula to use printf instead of heredoc format.
- **Status**: Need to rebuild and test.


## [2026-05-11] Implementing Multi-turn Agent Patrols
- **Issue**: Architect (and other agents) stuck in loops because gt-agent was stateless between patrols. Agents would read a file, get output, patrol would end, and the next patrol would start without the previous output in context.
- **Fix 1**: Updated gt-agent/internal/llm/client.go to support CompleteMessages (history).
- **Fix 2**: Refactored gt-agent/main.go to maintain a conversation history within each patrol cycle, allowing up to 10 turns.
- **Fix 3**: Relaxed the "EXACTLY ONE CMD:" rule in base.md.tmpl to encourage conversational efficiency.
- **Status**: Deployment pending (gt down, make install, gt up required).

- [x] Verified Multi-turn Loop: Architect successfully reads SPEC.md and writes architecture.md in a single patrol.
- [x] Fixed GT_SESSION Propagation: Exported session ID to agent commands to fix 'gt handoff' and 'gt done' detection.
- [x] Fixed Rig-Scoped Handoff: Updated internal/cmd/handoff.go to correctly resolve working directories for rig agents (Architect, QA, Mechanic, Planner).
- [x] Fixed Multi-Command Parsing: Updated gt-agent to correctly parse and execute multiple CMD blocks in a single LLM response.
- [x] Fixed `shiny` formula handoff: Updated `shiny.formula.toml` to explicitly target `mayor` in the handoff step, preventing self-handoff loops.
- [x] Fixed `gt done` normalization: Removed incorrect `-y` flag injection in `gt-agent` for the `gt done` command, which doesn't support it.
- [x] Upgraded Polecat capacity: Restarted `testgt2/rust` with `meta/llama-3.3-70b-instruct` to resolve Ralph Wiggum loops caused by the smaller `ministral-3:8b` model.
- [x] Fixed Handoff Loop: Updated all role templates (`architect`, `planner`, `qa`, `crew`) to include `mayor` as an explicit handoff target, breaking the recursive self-handoff loop.

- [x] Fixed `bd create` command in Planner template and `mol-idea-to-plan` formula: Removed unsupported `--formula` flag.
- [x] System Stabilization: Performed full `gt down`, `make install`, `gt up` sequence to clear Dolt contention and apply template fixes.
- [x] Verified Planner Progress: Planner successfully created task `hq-12q9.1-5` and handed off to the Mayor.
- [x] Proxy Model Routing Upgrade: Added `planner` to the massive model list in `freeride` proxy to prevent low-capacity hallucinations.
- [x] Dolt Health Optimization: Disabled Dolt metrics collection to eliminate CPU spikes and circuit breaker loops.
- [x] **Unit Test Recovery**: Resolved two regression failures:
    - `TestReadHookSessionID_AutoGeneratesFallback`: Fixed by explicitly clearing `GT_ROOT` and `GT_TOWN_ROOT` in the test environment to prevent interference from the real Gas Town installation.
    - `TestParseArchitect`: Fixed by manually registering the `te` prefix in the `PrefixRegistry` during the test, as the test environment does not initialize the global registry.
- [x] **Full Test Suite Green**: Confirmed `go test ./...` is 100% passing across all packages.

### 55. [FIXED] `gt formula run` Hardcoded "gastown" Rig Default
- **Files**: `internal/cmd/formula.go`, `internal/cmd/synthesis.go`
- **Issue**: `gt formula run` (and `gt synthesis start`) were hardcoded to default to the "gastown" rig when not invoked from a rig directory. This caused the Planner (which lives in the town root) to sling workflows to a non-existent "gastown" rig instead of the active rig (e.g., `testgt2`).
- **Fix**: Implemented `detectDefaultRig` helper that dynamically picks the first available rig from `rigs.json` if no rig is specified and the agent is not in a rig directory.
- **Status**: Fixed and Installed.

### 56. [FIXED] Planner and Mechanic Patrol Support
- **Files**: `internal/cmd/patrol_new.go`, `internal/cmd/patrol_report.go`
- **Issue**: `gt patrol new` and `gt patrol report` explicitly blocked the Planner and Mechanic roles, even though `gt prime` suggested these commands to them. This caused agents to loop on unsupported commands.
- **Fix**: Added `planner` and `mechanic` roles to the supported list for both commands.
- **Status**: Fixed and Installed.

### 57. [FIXED] Planner Patrol Loop Suppression
- **Files**: `internal/cmd/prime.go`, `internal/cmd/prime_molecule.go`
- **Issue**: The Planner was unconditionally shown patrol instructions (including `gt patrol report`) even when it had real project work (e.g., `shiny`) assigned on its hook. This confused the agent and sometimes blocked project instructions from being seen.
- **Fix**: Updated `outputMoleculeContext` to accept `hookedBead` and skip patrol instructions if any work is already hooked.
- **Status**: Fixed and Installed.

### 58. [FIXED] CMD-block parser swallows prose between commands (architect handoff stall)
- **File**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: When the LLM emitted multiple `CMD:` blocks separated by Markdown prose (e.g. `### Step 2`, `Since the role is …`, closing ` ``` ` fences), the in-`run()` parser left `inCmdBlock = true` and appended every subsequent line to the previous command. Closing fences were `continue`'d but never terminated the block. Result: the architect's `printf` ran with a multi-line tail of prose attached (`Since: not found`, `As: not found`, `EOF in backquote substitution`), and the follow-up `gt handoff mayor …` command was rewritten to include all trailing prose + a stray `-y`, so the handoff to mayor never reached the CLI.
- **Fix**: Extracted parser into testable `parseLLMResponse(response)` returning `(cmds, doneSummary, hallucinated)`. CMD blocks are now single-line by default. Heredocs (`cat <<'EOF' > file`) enter a strict heredoc mode that ends on a line whose trimmed content equals the captured terminator; unterminated heredocs are discarded rather than executed. Prose, headings, blank lines, and closing ` ``` ` fences no longer bleed into the previous command. Added `parseLLMResponse` + `detectHeredocTerm` helpers and 13 new unit tests including a direct regression for the architect handoff bug.
- **Status**: Fixed, tested (`go test ./cmd/gt-agent` green), built (v1.0.1-356-g5e2af996-dirty), deployed via `gt down`/`make install`/`gt up`.

### 59. [FIXED] Planner hallucinates fake `gt patrol*` commands; Witness/Refinery sling `shiny` formula at their own patrol bead
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/planner.md.tmpl`
- **Issue (planner)**: Logs showed the planner inventing `gt patrolling mol-planner-patrol cycle 24`, `gt patrol start … -c 24`, `gt patrol new … -f <url>`, and `gt patrol new --httpf "…"`. None of those subcommands or flags exist; the planner looped on stderr help text.
- **Issue (witness/refinery)**: The witness ran `gt sling hq-wisp-u0z1r --formula shiny` against its own `mol-witness-patrol` bead. The `shiny` engineering formula (design → implement → review → test → submit) has no business being bonded to a patrol bead, but no guard rejected it; `mol bond` failed with the bead already hooked and the agent went into an Extraordinary-action retry.
- **Fix**:
    1. Added `hasInvalidPatrolCommand` to `normalizeGeneratedCommand`. Allows only the real verbs (`new`, `report`, `scan`, `digest`) plus help flags; rejects `gt patrolling`, `gt patrol <unknown-verb>`, and the `-f` / `--httpf` / `-c` flag hallucinations for `gt patrol new` as harmless `true` NOPs.
    2. Added `hasInvalidShinyFormula` guard that consults a new `currentRole` package var (populated in `run()` from `roleCanonical`). When witness/refinery/mechanic/qa/planner attempts `gt sling … --formula shiny …`, it is rewritten to `true` with a stderr warning. Mayor/Architect/Crew/Polecat are unaffected.
    3. Updated `internal/templates/roles/planner.md.tmpl` to explicitly tell the planner: "If hook is empty AND inbox has no unread mail, you are idle. Do NOT invent commands. Output a single `DONE: idle` and stop." Also enumerates the four real `gt patrol` subcommands inline so the model has them in context.
- **Tests**: `TestHasInvalidPatrolCommand` (13 cases), `TestHasInvalidShinyFormula` (16 cases), `TestNormalizeRejectsWitnessShinyAndBadPatrol` (end-to-end through `normalizeGeneratedCommand`).
- **Status**: Fixed, tested, deployed (`gt down`/`make install`/`gt up`).

### [2026-05-11] Current State & Accomplishments
- **Status**: **STABLE & AUTONOMOUS**.
- **Accomplishments**:
    - **Rig Detection Fixed**: Town-level agents (Planner, Mayor) can now trigger formula-based workflows without manually specifying `--rig`.
    - **Patrol System Unified**: Planner and Mechanic now have full support for the `gt patrol` lifecycle, and patrol loops are suppressed when project work is active.
    - **E2E Flow Verified**: Architect -> Mayor -> Planner -> Mayor handoff loop is functional.
    - **High-Capacity Brains**: Architect, Mayor, and Planner are all confirmed to be using 70B+ models for reliable reasoning.
    - **Autonomous Signal Delivery**: NATS transport is reliably delivering signals between town and rig agents.

### 60. [FIXED] `gt handoff <role>` silently drops subject/message under NATS
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/architect.md.tmpl`, `internal/templates/roles/planner.md.tmpl`, `internal/templates/roles/qa.md.tmpl`, `internal/templates/roles/crew.md.tmpl`
- **Issue**: `gt handoff` was designed for in-process tmux session cycling. Under `GT_SESSION_TRANSPORT=nats` a cross-role handoff like `gt handoff mayor -s "Architecture ready" -m "Check inbox"` returns "provider does not support remote handoff" and silently discards `-s`/`-m`. Templates instructed agents to use this command, so the entire architect → mayor signal was dropping on the floor.
- **Fix**:
    1. Added `rewriteHandoffToMail` to `normalizeGeneratedCommand`. When the LLM emits `gt handoff <known-role> -s X -m Y`, the agent rewrites it to `gt mail send <role>/ -s X -m Y` (preserving quoting via `joinQuotedValue` / `shellQuote`). `--stdin` heredoc form is preserved.
    2. Updated architect/planner/qa/crew templates to use `gt mail send <target>/` + `gt nudge <target>` directly instead of `gt handoff`.
- **Tests**: `TestRewriteHandoffToMail`, `TestNormalizeRewritesArchitectHandoff`.
- **Status**: Fixed and deployed.

### 61. [FIXED] `gt sling` rejects targets with leading slash
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/mayor.md.tmpl`
- **Issue**: Mayor template used `{{ .RigName }}/architect` placeholders. When the town mayor rendered the template with `RigName=""`, the LLM saw `/architect` and dutifully ran `gt sling mol-polecat-work --on hq-wisp-r9ag /polecats --create` — which failed with `invalid target "/polecats": empty path segment at position 0`. The mayor then logged the error as an Extraordinary action and entered a recovery loop.
- **Fix**:
    1. Added `hasInvalidSlingTarget` guard in `normalizeGeneratedCommand` that scans positional arguments of `gt sling` and rewrites the command to `true` (with a stderr warning) if any starts with `/`.
    2. Updated `internal/templates/roles/mayor.md.tmpl` to use the literal `REAL-RIG/role` placeholder and added a "CRITICAL" warning showing correct vs. incorrect sling syntax (no leading slash).
- **Tests**: `TestHasInvalidSlingTarget`.
- **Status**: Fixed and deployed.

### 62. [FIXED] Snake-bracket placeholder `[command_for_item_X]` slips past placeholder guard
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: The planner emitted hallucinated commands like `[command_for_item_X]` and `[next_command_to_run]`. The existing `containsPlaceholder` only matched angle-bracket placeholders and a small allow-list of square-bracketed single words, so these went straight to the shell and produced `not found` errors.
- **Fix**: Extended `containsPlaceholder` with `snakeBracketPlaceholderRe` (`\[[A-Za-z][A-Za-z0-9]*(?:_[A-Za-z0-9]+)+\]`) which catches any `[snake_case_with_at_least_one_underscore]` pattern. Rejected as a NOP.
- **Tests**: `TestContainsPlaceholder_SnakeBracket`.
- **Status**: Fixed and deployed.

### 63. [FIXED] Mechanic stalls forever when any nudge arrives (LLM diversion)
- **Files**: `cmd/gt-agent/main.go`
- **Issue**: The mechanic has no LLM system prompt — it runs a hardcoded deterministic patrol script (`mechanicPatrolScript`). But the script path was only taken when `workItems == 0` after `await-signal` timed out. As soon as another agent nudged the mechanic (e.g. refinery: "Recovery initiated for Extraordinary action"), `workItems` became non-zero and the loop fell through to the LLM/multi-turn code path. With no template, no system prompt, and a stale `gt prime --hook` output, the mechanic just hung at "Processing 1 work item(s) (patrol #1)" with no further log output. Multiple stale nudges accumulated in the queue, so every restart immediately wedged.
- **Fix**: Added an unconditional mechanic short-circuit at the top of the main loop (immediately after `gatherWork`). The mechanic now always increments `PatrolCount`, prints work-item count, runs `mechanicPatrolScript`, saves state, sleeps 2s, and continues — regardless of how many nudges are queued. Nudges are still drained, just ignored as "work items" since the deterministic script already covers the mechanic's responsibilities.
- **Symptom signature**: `[gt-agent] Drained N nudge(s)` → `[gt-agent] Processing N work item(s) (patrol #1)` → no further output for minutes. Process alive (PID stable) but no LLM call, no patrol script, no exit.
- **Status**: Fixed and deployed. After the fix the log progresses to `[gt-agent] Running mechanic patrol script...` → patrol output → next cycle.

### 64. [FIXED] Two mechanics spawned (`hq-mechanic` + `te-<rig>-mechanic`); rig mechanic is redundant
- **Files**: `internal/cmd/up.go`, `internal/cmd/up_test.go`, `internal/cmd/status.go`, `cmd/gt-agent/main.go`
- **Issue**: `gt up` spawned both a town-level mechanic (`hq-mechanic`) and a per-rig mechanic (`te-<rig>-mechanic`) for every registered rig. Both ran the same deterministic patrol script. This doubled (or worse) the rate of "Mechanic detected Extraordinary action — please recover" nudges to every agent, contributing to the inbox bloat / recovery-loop pathology and the LLM-diversion bug in #63. `internal/beads/agent_ids.go` already declares mechanic as `TownLevelRoles`, so the per-rig mechanic was never canonical to begin with.
- **Fix**:
    1. Dropped `RoleMechanic` from `startRigAgentsWithPrefetch` (no more per-rig mechanic phase) and from the per-rig `services` aggregation loop in `runUp`.
    2. Removed the `case constants.RoleMechanic:` from `startRigAgentPhase`'s worker switch and deleted the now-dead `upStartRigMechanic`.
    3. Removed the per-rig mechanic entry from `gatherRigAgentDefs` in `status.go`.
    4. Added the town mechanic to the `agentDefs` slice at status.go:1731 (alongside mayor/deacon/planner) so `gt status` actually shows it. Previously the town mechanic was running but invisible to `gt status`.
    5. Relaxed `mechanicPatrolScript` to scan ALL recent agent logs instead of filtering by `*$current_rig*` (which was hardcoded to fall back to `"testgt2"` and excluded town-level logs entirely).
- **Tests**: Updated `TestStartRigAgentsWithPrefetch_EmptyRigs` and `TestStartRigAgentsWithPrefetch_RecordsErrors` to the new four-return signature.
- **Verification**: After deploy, `gt up` reports a single `✓ Mechanic: hq-mechanic` line and `gt status` shows one `🔧 mechanic` row at town level — no rig mechanic under `testgt2/`.
- **Status**: Fixed and deployed.

### 65a. [VERIFIED 2026-05-11 19:30] Planner end-to-end test on real epic
- **Test bead**: `hq-hkc` "Plan Hello API implementation tasks (test bed for fix #65)" — small description pointing at testgt2/architect/architecture.md and testgt2/mayor/rig/SPEC.md.
- **Procedure**: `bd close hq-eoi` (F23 fake), `gt sling hq-hkc planner`, `gt nudge planner`, monitor `logs/sessions/hq-planner.log` for 60s.
- **Results** (all PASS):
    - Planner read its hook (128 hq-hkc references), ran `gt bd show hq-hkc`, located parent.
    - Created **11 child tasks** with proper `--parent hq-hkc` linkage (hq-hkc.1 … hq-hkc.11), covering analysis / TDD setup / data model / endpoints / validation / tests / docs / load test.
    - Wired **9 dependencies** via `bd dep add` forming a sane TDD chain (`Analyze → Design → Implement → Tests`).
    - Sent `gt mail send mayor/ -s "Hello API Implementation Plan Complete"` and `gt nudge mayor`.
    - Regression counters all zero: 0 fake `F\d+/T\d+` IDs in shell, 0 `**Rationale**` markdown in shell, 0 new "Urgent: Resolve" mails, 0 surviving snake-bracket placeholders.
    - One snake-bracket placeholder hallucination was correctly **caught and rejected**: `bd dep <REPLACE_WITH_DEPENDENCY_IDS> hq-hkc` → `true`. Fix #62 working.
    - Bonus: on the next patrol cycle the planner couldn't find `architecture.md` (town planner has no rig context) and honestly mailed mayor `"Invalid Task: hq-hkc — specification and architecture documents are missing"` instead of inventing fake IDs. Exactly the safe-failure behaviour the hardened template targets.
- **Conclusion**: Fixes #58, #59, #62, #65 are confirmed working under live load with a town-level 70B planner against a real epic.

### 65. [FIXED] Same-line markdown bold prose bleeds into shell command (planner F23 hallucination)
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/planner.md.tmpl`
- **Issue**: Fix #58 stripped prose on *subsequent* lines but the planner started emitting same-line rationale tails like:
  ```
  CMD: bd search "F23" --priority-max "P0" --status "open" **Rationale for Command Change:**
  ```
  The trailing `**Rationale for Command Change:**` is on the same logical line as the command, so the single-line parser took it as part of the shell command and executed `bd search "F23" ... **Rationale for Command Change:**`, which bd cheerfully accepted as a search term: `No issues found matching 'F23 **Rationale for Command Change:**'`.
- **Deeper symptom**: The planner invented bead IDs entirely (`F23`, `T34`, `T67`, `T89`, `Patrol Cycle #55`). Real Gas Town bead IDs look like `hq-wisp-xxxx`, `hq-NNN.X-N`, or `te-wisp-xxxx`. The planner then sent `gt mail send mayor/ -s "Urgent: Resolve F23 to Unblock Tasks"` polluting mayor's inbox with fake urgent work, ran `bd show F23` (404), `bd search "F23" --all` (invalid flag), and finally `bd create --status "open"` (also invalid flag) — burning LLM turns and creating fake beads about a non-existent ID.
- **Fix**:
    1. Added `trailingMarkdownBoldRE` (`\s+\*\*[^*\n]*[A-Za-z][^*\n]*\*\*.*$`) and applied it after the fence-strip step in `parseLLMResponse`. Strips ` **anything containing letters**` and any trailing content from CMD lines. Carefully constructed so shell globstars (`ls **/*.go` — no whitespace before `**`) survive untouched.
    2. Updated `internal/templates/roles/planner.md.tmpl` with a new **HARD RULES — NO HALLUCINATIONS** section:
       - "NEVER invent bead/task IDs" with explicit examples of real-vs-fake formats.
       - "NEVER mail the mayor about work you cannot prove exists" — must have seen the ID in real `gt`/`bd` output during the same patrol.
       - "NEVER append a Markdown rationale paragraph to a CMD: line."
       - "NEVER invent flags" — points out `--all` is not on `bd search`, `--status` is not on `bd create`.
- **Tests**: 4 new `TestParseLLMResponse` subtests:
    - `trailing markdown bold prose on CMD line is stripped`
    - `trailing markdown bold followed by more prose is stripped`
    - `shell globstar **/* is preserved` (false-positive guard)
    - `trailing markdown bold with multiple **bold** segments strips from first`
- **Status**: Fixed and deployed.

### 66. [FIXED] Inline `CMD:` markers cram multiple commands onto one line
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: When the planner reached a "handoff" turn, the LLM emitted:
  ```
  CMD: bd update hq-bbn --priority 2 CMD: gt mail send mayor/ -s "ready" -m "go" CMD: gt nudge mayor "check" CMD: DONE: handed off to mayor
  ```
  All four CMDs on a single line. My existing parser only split on newlines, so it took the whole tail as arguments to `bd update`, producing `gt bd update hq-bbn --priority 2 CMD: gt mail send mayor/ -s "Hello API plan ready" -m "..."` and the shell choked on the unterminated quoted string.
- **Fix**:
    - Added `inlineCMDMarkerRE` (`\s+CMD:\s*`) and `splitInlineCMDs` helper.
    - In `parseLLMResponse`, after stripping fences and trailing markdown bold, split the residual command on inline `CMD:` markers and treat each non-empty fragment as a separate command.
    - A trailing `DONE: …` fragment from the split is consumed as `doneSummary` (not executed as a `DONE:` shell command).
    - Leading-whitespace requirement in the regex prevents accidental splits inside argument values (e.g. `echo "prefixCMD:notamarker"`).
    - Heredoc detection still runs on the FIRST sub-segment before the split, so multi-line heredocs aren't affected.
- **Tests**: 3 new `TestParseLLMResponse` subtests:
    - `inline CMD: markers split into separate commands`
    - `inline CMD: trailing DONE: is captured as summary`
    - `non-leading CMD: in argument is preserved`
- **Status**: Fixed and deployed.

### 67. [FIXED] `gt patrol report` flag hallucinations + `gd` → `gt` typo
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/planner.md.tmpl`
- **Issue (flags)**: After the planner handed off to mayor it kept emitting commands instead of stopping. Sample hallucination:
  ```
  CMD: gt patrol report --incident hq-bbn.3 --status "STALLED: Missing Critical Artifact"
       --summary "..." --recommendation "Await response from mayor/"
  ```
  Real `gt patrol report` only accepts `--summary` (required) and `--steps`. `--incident`, `--status`, `--recommendation`, `--sirius`, etc. are all fabricated. Fix #59 caught invalid VERBS but didn't validate FLAGS on `report`.
- **Issue (typo)**: Same planner cycle emitted `CMD: gd patrol report …` (capital-G typo for `gt`). Shell failed with `gd: not found` (exit 127).
- **Fix**:
    1. Extended `hasInvalidPatrolCommand`: for `verb == "report"`, walk remaining flags and reject any not in the allow-list `{--summary, -s, --steps, --help, -h, --json, --verbose, -v}`.
    2. Added a leading-`gd ` → `gt ` rewrite at the top of `normalizeGeneratedCommand` (before any other gt-specific rewriters), with negative-test coverage to ensure `gt patrol …`, `echo gd`, and `grep gd file` are not touched.
    3. Updated `internal/templates/roles/planner.md.tmpl`:
       - Renamed handoff step to **"HANDOFF + STOP"** with explicit instruction to emit `DONE: handed off to mayor` and stop after the nudge.
       - Documented that `gt patrol report` is NOT a status-update channel and lists the real flags.
       - Added a **"FILE PATH DISCIPLINE"** rule: when a bead description gives an absolute path, USE THAT EXACT PATH. Don't read `./architecture.md` (the planner had been reading a stale generic doc in its CWD instead of the rig-specific one).
- **Tests**: 6 new `TestHasInvalidPatrolCommand` subtests covering the four bogus flags and two valid forms; new `TestNormalizeRewritesGdTypo` with 6 cases.
- **Status**: Fixed and deployed.

### 77. [FIXED] `gt mail send planner/` fails as "unknown recipient" while `gt mail directory` lists it as valid
- **Files**: `internal/mail/resolve.go`, `internal/mail/resolve_test.go`
- **Issue**: Live in production after fix #76 redeployed mayor. Mayor LLM tried `gt mail send planner/ -s "..." -m "..."` and got:
  ```
  Error: unknown recipient: planner/
  ```
  But `gt mail directory` cleanly lists `planner/` as a valid agent address. Same with `mechanic/`. Without the slash (`gt mail send planner`) worked, but mayor's template uses the slash form to disambiguate.
- **Root cause**: `internal/mail/resolve.go`'s `validateAgentAddress` had a hand-rolled `switch` covering only `mayor`, `mayor/`, `deacon`, `deacon/`, `overseer` as town-level singletons. Addresses containing `/` that fall through the singleton check get parsed as `<rig>/<role>` — but `planner/` has no rig segment, so `len(parts) < 2 || parts[1] == ""` triggers and returns `ErrUnknownRecipient`. This is the exact same town-level-singleton omission we fixed in `agentIDToBeadID` (#71) and `validateAgentAddress`'s sibling list in status.go — just in a third file.
- **Fix**: Added `constants.RolePlanner + "/"`, `constants.RolePlanner`, `constants.RoleMechanic + "/"`, `constants.RoleMechanic` to the singleton allowlist. Added a comment pointing at the other two files so the next person adding a town-level agent will remember this third location.
- **Tests**: `TestValidateAgentAddress_TownLevelSingletons` pins `mayor`/`deacon`/`planner`/`mechanic` (with and without trailing slash) all validating, while bogus addresses still fail.
- **Lesson learned**: Three places in this codebase enumerate town-level singletons. There ought to be one. Filed as future work, not blocking.
- **Status**: Fixed and deployed.

### 76. [FIXED] Mayor stage-wraparound: `Architecture Ready` mail routes to Stage 1 (Design) instead of Stage 2 (Plan)
- **Files**: `internal/templates/roles/mayor.md.tmpl`, `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: Live reproducer immediately after fix #75 deployed. Mayor woke up, read the architect's `Architecture Ready` mail (subject=`Architecture Ready`, wisp=`hq-wisp-eulgk`, from=`testgt2/architect`), attached the wisp to its own hook, and then ran:
  ```
  CMD: gt sling shiny --on hq-wisp-eulgk testgt2/architect
  ```
  Stage 1 (Design), back to the architect, on the architect's OWN handoff mail. That sends the architect a "new" design assignment whose content is its own past notification — architect re-does design, re-mails mayor "Architecture Ready", mayor re-slings: infinite loop.
- **Root cause**: Mayor's template described stages as prose (`### Stage 1: Design`, `### Stage 2: Plan`, ...) and trusted the LLM to map incoming mail subjects to the correct stage. The LLM read `Architecture Ready` and pattern-matched on the word "Architecture" → picked the design formula. Also the mayor used the MAIL'S WISP as `--on`, not the actual project bead, compounding the loop.
- **Fix (two-layer)**:
    1. **Mayor template rewritten as a deterministic routing table.** Top-of-file mission statement says explicitly: "You are an event router, not a designer or planner." A subject→stage→command table is the primary instruction:

       | Signal | Stage | Command |
       |---|---|---|
       | New project bead, no `attached_formula` | 1. Design | `gt sling shiny --on <project-bead> <rig>/architect` |
       | `Architecture Ready`/`Complete` | 2. Plan | `gt sling mol-idea-to-plan --on <project-bead> planner` |
       | `Plan Complete`/`Plan Ready` | 3. Implement | `gt sling mol-polecat-work --on <task> <rig>/polecats --create` |
       | `POLECAT_DONE` | 4. Review | `gt sling code-review --on <bead> <rig>/qa` |
       | `QA Complete` | done | close convoy |
       | `BLOCKED:` | unblock | investigate, do NOT re-sling |

       New explicit warning: *"`Architecture` in a subject means architecture is FINISHED, not 'go do architecture'."* A dedicated `--on MUST be the PROJECT bead, never the handoff mail's wisp` section gives the correct vs wrong concrete example side-by-side.
    2. **Agent-layer guard `stageWraparoundReason`** in `gt-agent`: matches `gt sling shiny --on <bead> <rig>/architect`, looks up the bead's title via `bd show -o json`, and rejects if the title starts with any architect-handoff prefix (`Architecture Ready`, `Architecture Complete`, `Architecture location`, `Architecture Plan Submission`). Fails OPEN on any bd lookup error to avoid stranding legitimate work. Title lookup is injectable via the `beadTitleLookup` package var so tests can stub it (no Dolt round trip in CI).
- **Scope-discipline**: deliberately covers only the `shiny`→architect wraparound. Other potential stage wraparounds (`mol-idea-to-plan`→planner on a Plan Complete wisp, etc.) are left to the deterministic template; we add normalizer guards only after observing the bug in production, to keep false positives near zero.
- **Tests**: `TestStageWraparoundReason` with 12 cases — canonical loop variants (must block), legitimate project/SPEC beads (must NOT block), different formulas / different targets (out of scope), placeholder bead ID (fail open), unknown bead (fail open), non-sling commands (untouched). `TestIsArchitectHandoffTitle` pins the case-insensitive prefix matcher.
- **Cleanup**: closed `hq-wisp-eulgk` (the loop-trigger wisp) and unhooked the architect from it.
- **Status**: Fixed and deployed.

### 75. [FIXED] Agent inboxes grow unbounded — read mail never gets archived
- **Files**: `cmd/gt-agent/main.go`, `internal/cmd/mail_drain.go`, `internal/cmd/mail_drain_test.go`
- **Issue**: Mayor's inbox observed at 78 messages / 31 unread within a few hours of running. Symptoms repeated for every other agent. Root cause was two-layered:
    1. **Nobody ran `gt mail drain` automatically.** The drain command already existed, knew how to archive read wisps + old protocol messages, but had to be invoked manually. Autonomous agents never invoked it, so read mail piled up forever.
    2. **`drainableSubjects` allowlist was too narrow** — it only covered protocol notifications (POLECAT_DONE, MERGED, LIFECYCLE:*, etc.). The Gas Town pipeline's most common notifications — `Architecture Ready`, `Plan Complete`, `QA Complete`, `BLOCKED:` — weren't in the list, so even when drain ran they were ignored.
- **Fix (two-layer)**:
    1. **`mail_drain.go`**: introduced `drainableHandoffSubjects` for the agent handoff subjects (`Architecture Ready`, `Architecture Complete`, `Architecture location`, `Architecture File Status`, `Architecture Plan Submission`, `Plan Complete`, `Plan Ready`, `Plan in progress`, `QA Complete`, `QA Ready`, `Review Complete`, `BLOCKED:`). Added `classifyDrainableSubject` returning `"protocol"`, `"handoff"`, or `""`. The drain loop now gates handoff-subject drains on `msg.Read==true` (or `--all`) so we never silently drop a critical unread "Architecture Ready" before mayor has acted on it. Protocol subjects keep the old age-only drain.
    2. **`cmd/gt-agent/main.go`**: each patrol cycle now ends with `gt mail drain --max-age 10m`. Non-fatal — if drain errors we log and continue. 10m is short enough to keep inboxes tidy (agents cycle every ~30s, so each message gets ~20 cycles to be acted on) and long enough that a real handoff that mayor needs to read still survives.
- **Behavior summary**:
    - Read mail older than 10m: archived automatically.
    - Unread mail: kept forever (no silent loss).
    - `🤝 HANDOFF` and `HELP:*` subjects: preserved (require human attention).
    - Operators can still bulk-archive with `gt mail drain --all` or surgical `gt mail archive <id>`.
- **Tests**: extended `TestIsDrainableMessage` with the new handoff subjects; new `TestClassifyDrainableSubject` pins the protocol-vs-handoff split.
- **Help text**: `gt mail drain --help` now documents the two-tier drain rules and the Read-gate on handoff subjects.
- **Status**: Fixed and deployed.

### 74. [FIXED] Architect overwrote 157-byte architecture.md with 0 bytes (heredoc placeholder literally rendered as content)
- **Files**: `internal/templates/roles/architect.md.tmpl`, `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: Architect's `cat > .../architecture.md <<'EOF' ... EOF` heredoc from fix #72's template was rendered literally by the LLM: it pasted `...` as the body, producing two 0-byte files. The architect then ran `gt unhook --force` and emitted `DONE: architecture handed off to mayor` without ever mailing mayor or writing real content. Pipeline stalled silently — mayor had no idea architecture was "ready" because no fresh handoff mail was sent, and the spec on disk was gone.
- **Fix (two-layer)**:
    1. **Architect template** rewritten end-to-end:
       - Step 1 `cat SPEC.md` is now mandatory (read SPEC even if architecture.md already exists).
       - Step 2 heredoc uses `<INSERT-REAL-ARCHITECTURE-HERE>` as the placeholder marker (explicitly not `...`) inside section headings.
       - **New Step 3 — VERIFY**: `wc -c architecture.md` must report ≥ 200 bytes AND `grep -c 'INSERT-REAL-ARCHITECTURE-HERE'` must be 0 before proceeding.
       - Mirror to rig-root only AFTER verification (Step 4).
       - Mail mayor + nudge (Step 5).
       - `gt unhook` is now Step 6 with explicit "ORDER MATTERS — never unhook before steps 1-5 succeed".
       - New "HARD RULES" block at top of file reinforces all of the above before any command runs.
    2. **Agent-layer guard `isEmptyContentFileHeredoc`** in `gt-agent`: rejects shell commands of the form `cat > <…/architecture.md|design.md|plan.md|SPEC.md|README.md> <<EOF ... EOF` if the heredoc body is empty, whitespace-only, contains only literal `...`, or contains only unreplaced placeholder tokens (`<INSERT-…>`, `<TODO>`, `<FIXME>`, `<YOUR-…>`). Heading-only bodies (`## API\n## Data Model`) also fail the check since they produce a useless skeleton. Match logic uses an opening-regex + manual delimiter scan (Go RE2 has no backreferences).
- **Tests**: `TestIsEmptyContentFileHeredoc` with 10 cases — empty body, `...` body, INSERT-placeholder body, TODO-only body, real content, short-but-real content, unquoted EOF, non-content-file (out of scope), non-heredoc, headings-only, mixed placeholder+real.
- **Status**: Fixed and deployed.

### 73. [FIXED] `agentIDToBeadID` returns "" for `<rig>/architect` and `<rig>/qa`; planner template has empty `{{ .RigName }}`
- **Files**: `internal/cmd/sling_helpers.go`, `internal/cmd/sling_helpers_test.go`, `internal/templates/roles/planner.md.tmpl`
- **Issue**: Two sibling bugs uncovered while watching fix #70 land in production:
    1. **`agentIDToBeadID` had an explicit "I don't handle this" branch** for architect/qa:
       ```go
       case len(parts) == 2 && (parts[1] == "architect" || parts[1] == "qa"):
           return ""
       ```
       That dead branch presumably dated from before `ArchitectBeadIDWithPrefix` / `QABeadIDWithPrefix` existed. Now both functions exist (see `internal/beads/agent_ids.go:368-385`) but nothing routed to them. Live consequence: architect emitted `Summary: architecture handed off to mayor` cleanly, the auto-unhook fired, but `gt unhook` then died with `Error: could not convert agent ID testgt2/architect to bead ID`. Architect stayed hooked on its own outgoing `hq-wisp-6k4y "Architecture Ready"` wisp and re-mailed mayor every patrol — observed 55 duplicate "Architecture Ready" entries in mayor's inbox.
    2. **Planner template hard-coded `{{ .RigName }}`** in canonical search paths, but the planner is a **town-level** agent so `.RigName` is empty. The deployed one-liner expanded to `/home/stevef/gt//architect/architecture.md` (double slash) and resolved against the town root by accident.
- **Fix**:
    1. **`agentIDToBeadID`**: routed `<rig>/architect` to `beads.ArchitectBeadIDWithPrefix(prefix, rig)` and `<rig>/qa` to `beads.QABeadIDWithPrefix(prefix, rig)` (same shape as the witness/refinery cases right below). Dead `return ""` removed.
    2. **Planner template §1a (canonical search)**: rewritten to iterate over every rig in `rigs.json` instead of substituting the planner's own (empty) rig. The new copy-paste one-liner is:
       ```bash
       for rig in $(jq -r 'keys[]' {{ .TownRoot }}/rigs.json 2>/dev/null); do
         for p in {{ .TownRoot }}/$rig/architect/architecture.md \
                  {{ .TownRoot }}/$rig/architecture.md \
                  {{ .TownRoot }}/$rig/mayor/rig/architecture.md; do
           if [ -s "$p" ] && ! grep -q "Awaiting content" "$p"; then
             echo "FOUND:$p"; cat "$p"; break 2
           fi
         done
       done
       ```
       Hard rule added: do NOT use `{{ .TownRoot }}//architect/architecture.md` (empty rig segment) — town root is stale-fixture territory.
- **Tests**: new `TestAgentIDToBeadID_RigLevelArchitectQA` covering `<rig>/architect`, `<rig>/qa` (both with and without trailing slash, multiple rigs); explicitly checks for non-empty return because empty was the bug.
- **Status**: Fixed and deployed. Architect's stuck `hq-wisp-6k4y` hook cleared by `gt down`/`gt up`; mayor's 55 duplicate "Architecture Ready" mails still in inbox, can be archived with `gt mail archive --stale`.

### 72. [FIXED] Mayor strips `architect/` from architecture path; Planner stubs the file and hallucinates 21+ tasks
- **Files**: `internal/templates/roles/architect.md.tmpl`, `internal/templates/roles/planner.md.tmpl`, `internal/templates/roles/mayor.md.tmpl`
- **Issue**: Three cascading bugs in one observed pathology on `hq-wisp-zvlv`:
    1. **Mayor used `gt mail send` instead of `gt sling`** to delegate planning. The wisp had `msg-type:notification` label and Subject `Architecture location: /home/stevef/gt/testgt2/architecture.md`. There was no `mol-idea-to-plan` formula behind it; mayor manually crafted a notification mail and the planner treated it as work.
    2. **Mayor stripped `architect/` from the architecture path.** Architect's outgoing mail said `architecture.md created at /home/stevef/gt/testgt2/architect/architecture.md` (correct). Mayor's outgoing wisp said `Architecture location: /home/stevef/gt/testgt2/architecture.md` (wrong — missing `architect/` segment). Classic LLM "path simplification".
    3. **Planner created a 58-byte stub at the bad path** ("Architecture file created. Awaiting content for analysis.") to "fix" the missing file, then planned 21 child tasks against the stub. The children were pure hallucinations (e.g. `hq-wisp-zvlv.1` "Setup Q4 material inference directory structure", `.5` "Integrate material VBA with Excel 2023", `.4` "QA protocol for material classifications") with no relation to the real FastAPI Hello World spec — the planner invented an entire fictitious project from the subject line of a notification mail.
- **Fix (defense-in-depth across all three layers)**:
    1. **Architect template**: writes `architecture.md` to BOTH canonical locations (`{{ .WorkDir }}/architecture.md` AND `{{ .TownRoot }}/{{ .RigName }}/architecture.md`) and quotes both paths in its handoff mail. Even if mayor strips the subdir, the rig-root path resolves to the real spec.
    2. **Planner template (the biggest change)**: new section **1a. FIND THE REAL ARCHITECTURE FILE — DO NOT CREATE STUBS**:
       - Try the absolute path from the bead first.
       - If missing OR stub (size <200 bytes OR contains "Awaiting content"), search canonical fallbacks in order: `<rig>/architect/architecture.md` → `<rig>/architecture.md` → `<rig>/mayor/rig/architecture.md` → `<workdir>/architecture.md`.
       - Includes a copy-pasteable shell one-liner the model can run verbatim.
       - **Hard rule: NEVER create `architecture.md` yourself.** Documented inline with the actual observed pathology (the 21 fake tasks).
       - If none found, mail mayor "BLOCKED: architecture missing", unhook, emit `DONE: blocked - architecture missing`, stop. No fake child beads.
    3. **Mayor template**: new "CRITICAL — DELEGATE WITH `gt sling`, NEVER `gt mail send`" block. Spells out that `gt sling mol-idea-to-plan --on <bead-id> planner` is the ONLY correct way; `gt mail send planner/ -s "Architecture location: ..."` is forbidden; paths must not be pasted into subject/body (let planner discover); idle is preferable to inventing work.
- **Status**: Fixed and deployed. Cleanup of `hq-wisp-zvlv` stub + 21 fake children left to operator (these are inert and harmless once mayor stops re-mailing the wrong path).

### 71. [FIXED] `agentIDToBeadID` doesn't know about planner/mechanic — breaks `gt unhook` for town-level singletons
- **Files**: `internal/cmd/sling_helpers.go`, `internal/cmd/sling_helpers_test.go`
- **Issue**: Discovered while verifying fix #70 live. `gt unhook` (and `gt hook clear`, `gt unsling`) was failing for the planner with:
  ```
  Error: could not convert agent ID planner/ to bead ID
  ```
  Root cause in `agentIDToBeadID(agentID, townRoot)`: it had hand-rolled cases only for `mayor` and `deacon`. Anything else fell through to the path-split, which for the bare string `planner` produced `len(parts) < 2` and returned "". `resolveSelfTarget()` returns `planner/` for the town-level planner singleton, so the lookup always failed. Same bug affected mechanic.
- **Symptom**: fix #70's auto-unhook fired correctly (log line `Handoff-style role finished — auto-unhooking...`) but the actual `gt unhook` exec'd by gt-agent immediately died with the above error. The hook didn't clear, and the cycle's "Extraordinary action detected" set up the next loop to repeat.
- **Fix**: Extended `agentIDToBeadID`'s town-level switch to also cover `planner` → `beads.PlannerBeadIDTown()` and `mechanic` → `beads.MechanicBeadIDTown()` (matching the four town-level singletons already known to `internal/cmd/status.go` and `internal/doctor/agent_beads_check.go`). Comment added so the next person adding a town-level agent remembers to update this table.
- **Tests**: `TestAgentIDToBeadID_TownLevelAgents` pins all four singletons (`mayor`, `deacon`, `planner`, `mechanic`) both with and without the trailing slash that `resolveSelfTarget` returns.
- **Status**: Fixed and deployed.

### 70. [FIXED] Planner/Architect/QA loop on the same bead forever (hook never clears after handoff)
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`, `internal/templates/roles/planner.md.tmpl`, `internal/templates/roles/architect.md.tmpl`, `internal/templates/roles/qa.md.tmpl`
- **Issue**: Single-shot roles (planner / architect / qa) all share a "do-once, hand off, stop" contract. But nothing ever cleared their hook after handoff. Concrete observed state on `hq-bbn` "Plan FastAPI Hello World":
    - Planner cycled the same bead 9+ times.
    - Created 41 child tasks under it (e.g. `Implement FastAPI app` showing up as `hq-bbn.18`, `hq-bbn.20`, `hq-bbn.41` — three near-identical duplicates).
    - Sent 9+ "Plan Complete" / "FastAPI Hello World Plan Ready" mails to mayor (`hq-wisp-0ik0`, `hq-wisp-fmmi`, `hq-wisp-va2y`, `hq-wisp-vh56`, `hq-wisp-pwqi`, `hq-wisp-3zqc`, `hq-wisp-vnef`, …) drowning mayor's inbox.
    - Mayor couldn't distinguish "real" handoff from re-spam; ended up reading 12+ unread mails and never slung to a polecat.
    - **Same pattern hit architect**: it hooked its own outgoing `hq-wisp-bjr5 'Architecture Ready'` bead and kept re-mailing mayor every patrol.
- **Fix (two-layer, belt + suspenders)**:
    1. **Template (explicit)**: planner/architect/qa templates now have a `**UNHOOK + STOP**` step that tells the agent to run `gt unhook` after `gt nudge mayor` and BEFORE emitting `DONE: …`. Reasoning is spelled out: "if you don't clear the hook, every subsequent patrol you'll see the same bead, re-mail mayor, and flood the inbox."
    2. **Agent safety net (implicit)**: after each cycle, if `shouldAutoUnhookAfterHandoff(role, summary)` returns true, the agent runs `gt unhook` itself. This catches cases where the model forgets the explicit step.
       - **Important**: the unhook is intentionally NOT gated on `!extraordinary`. Observed in production after the first attempt: planner emitted `Summary: handed off to mayor` cleanly, but a single failed sub-command earlier in the cycle (e.g. a `gt hook` typo) set `extraordinary = true`, which suppressed the unhook, and the loop kept re-firing the handoff. The keyword filter inside `shouldAutoUnhookAfterHandoff` is the real gate — it already rejects summaries containing `blocked`, `error`, `failed`, `missing`, `investigating`, etc. If the agent self-reports successful handoff, trust it.
- **`shouldAutoUnhookAfterHandoff` heuristic**:
    - Role must be in `handoffRoles = {planner, architect, qa}`. Mayor / Witness / Refinery / Mechanic / Deacon / Polecat / Crew are excluded (they have rolling workloads).
    - Summary must contain a positive handoff keyword: `handed off`, `hand off`, `handoff`, `plan complete`, `plan ready`, `design complete`, `design ready`, `architecture ready`, `architecture complete`, `review complete`, `qa complete`, `qa ready`.
    - Summary must NOT contain a failure / progress keyword: `unable to proceed`, `missing`, `stalled`, `blocked`, `error`, `failure`, `failed`, `investigating`, `in progress`.
    - Empty summary never triggers.
- **Tests**: `TestShouldAutoUnhookAfterHandoff` with 21 cases — happy paths for all three roles, all the negative keyword variants, empty summaries, and explicit pass-through for non-handoff roles (mayor / witness / refinery / mechanic / deacon / polecat / crew).
- **Status**: Fixed and deployed.

### 69. [FIXED] Normalizer kills real `gt hook` subcommands (`attach`/`detach`/`show`/`status`/`clear`)
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: The normalizer had a blanket `if hasCommandPrefix(trimmed, "gt hook") { return "gt hook", true }` rule that collapsed every `gt hook <anything>` to bare `gt hook`. This silently broke real subcommands. Live mayor log showed:
  ```
  [gt-agent] $ gt hook attach hq-wisp-5zi3
  ↓ (rewritten)
  [gt-agent] $ gt hook
  ```
  After the rewrite the mayor saw "Nothing on hook" instead of the wisp it just tried to attach, so it could never advance the workflow.
- **Fix**: Replaced the blanket collapse with subcommand-aware logic. If `gt hook` is followed by a recognized verb (`show`, `attach`, `detach`, `clear`, `status`), pass through verbatim. Bare `gt hook` and unrecognized trailing args still collapse (so hallucinated `gt hook patrolReport fooBar` still becomes a safe `gt hook`).
- **Tests**: `TestNormalizePreservesGtHookSubcommands` with 8 cases covering all valid subcommands, bare form, and an unknown-arg hallucination.
- **Status**: Fixed and deployed.

### 68. [FIXED] Mayor slings to bare `<rig>/polecats` (CLI requires a specific polecat name)
- **Files**: `cmd/gt-agent/main.go`, `cmd/gt-agent/main_test.go`
- **Issue**: After the planner's clean handoff, the mayor tried to dispatch implementation work with:
  ```
  CMD: gt sling mol-polecat-work --on hq-bbn testgt2/polecats --create
  ```
  The CLI rejects this — `testgt2/polecats` requires a specific polecat name (e.g. `testgt2/polecats/toast`) or you use bare `testgt2` to auto-spawn. The mayor self-corrected on the next turn, but that's a wasted LLM call.
- **Fix**: Added `rewriteBareRigPolecats` normalizer that rewrites positional sling targets ending in literal `/polecats` (single-segment, NOT `/polecats/<name>`) to bare `<rig>`. Preserves intent (auto-spawn) and saves the corrective round-trip.
- **Quote-aware**: The helper tracks balanced `"` chars across `strings.Fields` tokens to avoid rewriting matches that occur inside a quoted multi-word flag value (e.g. `--message "see testgt2/polecats for context"`).
- **Specific polecat preserved**: `gt sling … testgt2/polecats/toast` is NOT rewritten — that's a valid CLI target.
- **Tests**: `TestRewriteBareRigPolecats` with 6 cases: bare rewrite, two-positional variants, specific-polecat passthrough, plain-rig no-op, non-sling no-op, and the quoted-value false-positive guard.
- **Status**: Fixed and deployed.

## Open Issues (next session)

### Witness/Refinery inbox bloat — informational mail accumulating, no triage
- **Symptom**: `gt mail inbox` for `testgt2/witness` showed 40 messages, all status pings from `testgt2/refinery`: `MERGE_QUEUE_EMPTY`, `MERGE_QUEUE_NONEMPTY`, `MERGE_READY <wisp>`, `MERGE_ACTIVE <wisp>`, `MERGE_FAILED <wisp>`, `REFINERY_STATUS`, `NO_POLECATS_FOUND testgt2`, plus their `RE: …` replies. The witness reads `gt mail inbox`, sees a wall of informational mail with nothing actionable, and loops every patrol re-reading the same 40 messages without archiving or summarising. Hook is empty (`Nothing on hook - no work slung`) so the witness has no actual work, but the noisy inbox makes it look busy.
- **Root causes (hypothesised, needs investigation)**:
    1. Refinery sends a status mail every patrol cycle even when nothing has changed (`MERGE_QUEUE_EMPTY` repeated). These should be either NATS-only signals (no permanent bead) or deduplicated.
    2. Witness template doesn't tell the agent to archive/process refinery status mail in bulk.
    3. `gt mail send` for routine status creates a permanent bead + Dolt commit (per `CLAUDE.md`), which is exactly what the project convention says NOT to do for routine traffic — `gt nudge` should be used instead.
- **Proposed fix paths** (pick one or combine):
    a. Convert refinery's `MERGE_*` and `REFINERY_STATUS` to `gt nudge` instead of `gt mail send` (lowest impact, matches `CLAUDE.md` guidance).
    b. Add a witness normalizer: any inbox message from `<rig>/refinery` with subjects matching `MERGE_*|REFINERY_STATUS|NO_POLECATS_FOUND` is auto-archived after read.
    c. Suppress duplicate status mail at refinery: only send when state transitions.
- **Status**: NOT YET FIXED — logged for next session.
