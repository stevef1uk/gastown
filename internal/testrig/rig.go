// Package testrig provides generic rig names for unit tests only (not production rigs).
package testrig

const (
	// Name is the default rig identifier in tests.
	Name = "mockrig"
	// Alt is a second rig name when tests need two rigs.
	Alt = "mockrigb"
)
